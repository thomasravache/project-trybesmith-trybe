enum StatusCodes {
  OK = 200,
  BAD_REQUEST = 400,
  UNPROCESSABLE_ENTITY = 422,
  CREATED = 201,
  UNAUTHORIZED = 401,
  INTERNAL_SERVER_ERROR = 500,
  NOT_FOUND = 404,
}

export default StatusCodes;
